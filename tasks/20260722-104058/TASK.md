# IDEATION: user-configurable styling/theming for the web UI (deferred)

- STATUS: CLOSED
- PRIORITY: 5
- TAGS: frontend,styling,ideation,wontdo
- KIND: TASK
- FLOW STEP: DONE
- PLAN STATUS: APPROVED

## Goal (IDEATION ONLY - do not implement until much later)

Explore user-configurable styling/theming for the web UI: let the user pick or
define the palette/typography (e.g. import from a terminal theme), rather than a
single hard-coded scheme.

## Why

User feedback (2026-07-22): "really interesting but low priority would be
configurable style somehow, but that would be just an ideation task that we will
not implement until really late". Explicitly deferred - captured so it is not
lost.

## Notes

- Ideation deliverable only: a short design sketch (where themes live, how a user
  supplies one, how the CSS variables get parameterized). No implementation.
- Sibling of the broad styling pass task (that one does the fixed refinement;
  this one is the far-future configurability).
